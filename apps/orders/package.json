{
  "name": "@mercado/orders",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "migrate": "dotenv -e .env.local -- pnpm prisma migrate dev",
    "reset": "dotenv -e .env.local -- pnpm prisma migrate reset"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@mercado/orders": "link:",
    "@prisma/client": "^6.12.0",
    "prisma": "^6.12.0"
  },
  "devDependencies": {
    "dotenv-cli": "^8.0.0"
  }
}
